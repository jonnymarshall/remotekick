<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <% if yield(:title).empty? %>
      <title>Wifi Warriors</title>
    <% else %>
      <title><%= yield(:title)%></title>
    <% end %>

    <%= csrf_meta_tags %>
    <%= action_cable_meta_tag %>
    <%= stylesheet_link_tag 'application', media: 'all' %>
    <%#= stylesheet_pack_tag 'application', media: 'all' %> <!-- Uncomment if you import CSS in app/javascript/packs/application.js -->

    <!-- autoComplete.js stylesheet -->
    <link rel="stylesheet" type="text/css" media="screen" href="https://cdn.jsdelivr.net/npm/@tarekraafat/autocomplete.js@7.1.3/dist/css/autoComplete.min.css">
  </head>
  <body id="bootstrap-overrides">
    <%= render 'shared/navbar' %>
    <%= render 'shared/flashes' %>
    <%= yield %>

    <script src="https://cdn.jsdelivr.net/npm/@tarekraafat/autocomplete.js@7.1.3/dist/js/autoComplete.min.js"></script>

    <%= javascript_include_tag 'application' %>
    <%= javascript_pack_tag 'application' %>

  </body>
</html>
